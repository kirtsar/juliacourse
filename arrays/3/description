Напишите функцию с именем interior, которая принимает на вход массив и возвращает новый массив, содержащий все элементы, кроме первого и последнего из исходного массива.

Пример:
julia> t = [1, 2, 3, 4];
julia> print(interior(t))
[2, 3]

julia> t = [1];
julia> print(interior(t))
[]

