Напишите функцию cumulsum, которая принимает на вход массив чисел и возвращает массив кумулятивных сумм:
массив, в котором i-тый элемент равен сумме первых i элементов поданного на вход массива.

Пример:
julia> t = [1, 2, 3];
julia> print(cumulsum(t))
Any[1, 3, 6]

